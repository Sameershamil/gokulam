# Example environment variables - copy this to .env and fill in values!
# Note: Public variables (accessible on frontend) must start with PUBLIC_

# Server-side only variables (never exposed to frontend)
ADMIN_PASSWORD=gokulam2024
# DATABASE_URL=postgresql://user:pass@localhost:5432/gokulam

# Public variables (exposed to frontend)
PUBLIC_APP_NAME="Gokulam Holidays"
